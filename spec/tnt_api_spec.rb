require "spec_helper"

RSpec.describe TntApi do
  it "has a version number" do
    expect(TntApi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
